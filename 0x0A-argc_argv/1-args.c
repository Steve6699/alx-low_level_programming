#include <stdio.h>

/**
 * main - print number of arg passed to program
 * @argc: number of arg
 * @argv: array of string arg
 * Return: 0 if success
*/

int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
